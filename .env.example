# Optional in development: leave unset to proxy /api to the local contact API (see vite.config.mjs).
# Set this only when you want the frontend to call a remote API directly during dev.
# VITE_CONTACT_API_URL=http://localhost:3000
# VITE_CONTACT_API_URL=https://champion-service.vercel.app
